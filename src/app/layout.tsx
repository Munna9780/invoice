import type { Metadata } from 'next';
import { Inter } from 'next/font/google';
import './globals.css';

const inter = Inter({ subsets: ['latin'] });

export const metadata: Metadata = {
  title: 'File Converter - Convert Any File Format',
  description: 'Free online file converter. Convert PDF, images, audio, video, and spreadsheets to various formats.',
  keywords: 'file converter, pdf converter, image converter, audio converter, video converter, spreadsheet converter',
};

export default function RootLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  return (
    <html lang="en">
      <body className={inter.className}>
        <main className="min-h-screen bg-gray-50">
          {children}
        </main>
      </body>
    </html>
  );
} 